{
  "name": "vip-yemen",
  "version": "1.3.0",
  "private": true,
  "scripts": {
    "install": "cd website && bun install",
    "build": "cd website && bun run build",
    "dev": "cd website && bun run dev",
    "start": "cd website && bun run start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
