# ViP Yemen v1.0.5

## إصلاح الأصول والأيقونات

استبدلت هذه النسخة أيقونة Capacitor الافتراضية التي كانت موجودة في مجموعة iOS بأيقونة ViP Yemen الذهبية الرسمية ذات النسر وVIP، مع الحفاظ على ملف `Contents.json` ومقاس App Store المطلوب `1024x1024`.

## التحقق

تم فحص موارد Android في كثافات mdpi وhdpi وxhdpi وxxhdpi وxxxhdpi، ونسختي `ic_launcher` و`ic_launcher_round`، والتحقق من وجود أيقونة iOS الرسمية. كما تم اختبار قواعد عرض المربع الأصفر والصورة الرئيسية في الموقع على سطح المكتب والهاتف لمنع القص والتراكب.

## الترقية

يحافظ الإصدار على `applicationId` `com.vipyemen.app`، ويستخدم `versionName 1.0.5` و`versionCode 1000005` حتى يُعامل كتحديث فوق النسخ السابقة عند استخدام توقيع الإنتاج نفسه.

## ملاحظة النشر

ملف APK المرفق موقّع debug للاختبار المباشر. ملف AAB يحتاج إلى توقيع الإنتاج الرسمي قبل نشره في Google Play، وملف iOS يحتاج إلى البناء والتوقيع عبر Codemagic أو Xcode باستخدام حساب Apple Developer.
